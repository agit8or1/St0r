{
  "version": "3.2.95",
  "releaseDate": "2026-06-17",
  "changelog": [
    "Fix: disabled file/image backups now reliably show 'Disabled' instead of 'Failed'. St0r now reads UrBackup's authoritative file_disabled/image_disabled status flags (which resolve group and global inheritance server-side) instead of only the per-client settings rows, which missed disables inherited from a group (issue #14)"
  ]
}
