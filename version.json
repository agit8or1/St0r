{
  "version": "3.2.94",
  "releaseDate": "2026-06-15",
  "changelog": [
    "Fix: Email & Alerts settings now persist — mail settings are saved via UrBackup's mail endpoint (not the general endpoint that silently dropped them), and Pushover/report settings are stored locally (issue #13)",
    "Fix: clients with file or image backups disabled now show 'Disabled' instead of 'Failed', and no longer count as needing attention (issue #14)"
  ]
}
